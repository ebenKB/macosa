import { get } from '@ember/object';
import ApplicationAdapter from '../application/adapter';

export default ApplicationAdapter.extend({

  // urlForCreateRecord() {
  //   return get(this, 'host') + '/company_types';
  // }
});
