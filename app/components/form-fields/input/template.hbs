  {{!-- {{input
      class=classNames
      type=type
      placeholder=placeholder
      keyUp=(action "validate" changeset)
      value=(get changeset property)
      changeset=changeset
  }} --}}


{{input
  class=classNames
  type=type
  placeholder=placeholder
  keyUp=(action "validate" changeset)
  change=(mut (get changeset property))
  value=(get changeset property)
  changeset=changeset
  property=property
  disabled=disabled
}}

  
{{!-- {{#if (get changeset.error property)}}
  <small>
    <ul style="margin:5px 0 0 0px;font-size: 13px; display: block" class="input-error">
      {{#each (get (get changeset.error property) "validation") as |message|}}
        <li>{{message}}</li>
      {{/each}}
    </ul>
  </small>
{{/if}} --}}
