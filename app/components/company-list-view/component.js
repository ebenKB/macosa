import Component from '@ember/component';
import { get } from '@ember/object';

export default Component.extend({
  actions: {
    // delete(company) {
    //   console.log('this is the company', company);
    //   get(this, 'delete')(company);
    // }
  }
});
